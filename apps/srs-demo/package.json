{
  "name": "@gll/srs-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:all": "concurrently \"pnpm --filter @gll/server dev\" \"vite\"",
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit",
    "preview": "vite preview",
    "e2e": "bddgen test && playwright test"
  },
  "dependencies": {
    "@gll/api-contract": "workspace:*",
    "@gll/srs-engine-v2": "workspace:*",
    "@gll/srs-shelving": "workspace:*",
    "vue": "^3.5"
  },
  "devDependencies": {
    "@playwright/test": "^1",
    "@vitejs/plugin-vue": "^5",
    "concurrently": "^9",
    "playwright-bdd": "^9",
    "typescript": "^5.7",
    "vite": "^6",
    "vue-tsc": "^2"
  }
}
