# current-focus

**Updated**: 20260313T000000Z

EP15 is **Impl-Complete**. All 3 stories done on `feature/EP15`.

- ST01: `@gll/api-contract` wire types updated (`choices`, `selectedKey`, `submittedKey`/`correctKey`, `SeedPayload`)
- ST02: `POST /batch` — distractor generation, pool validation, `BatchEntry` registry
- ST03: `POST /answers` — server-side correctness via `selectedKey`; `POST /seed` — returns `deckId`; E2E tests updated

**Next**: Push branch + open PR to main. EP16 (quiz runner HTTP rewrite) is the successor epic.
